sudo: required
dist: trusty

language: cpp

branches:
  only:
    - travis_test
    
addons:
  apt:
    packages:
      - qtbase5-dev

notifications:
    on_failure: never
    on_success: never
    
before_script:
  - mkdir build
  - cd build
  - cmake ..

script: make
